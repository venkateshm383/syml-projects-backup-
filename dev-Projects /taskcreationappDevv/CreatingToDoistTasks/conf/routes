# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)


POST  /doclist	     		controllers.Application.creatingTasks()

POST  /referaldata		controllers.Application.creatingReferralTasks()

POST  /leaddata		controllers.Application.creatingLeadTasks()

POST  /stageMaildata	     		controllers.Application.creatingStageMailTasks()

POST  /credit	     		controllers.Application.createTasksForCreaditInfromation()